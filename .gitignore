node_modules
dist
# O Playwright escreve aqui quando é invocado da RAIZ em vez de `e2e/` — o
# .gitignore de lá só cobre o caminho relativo a ele.
/test-results/
# E o estado de login vai junto, pelo mesmo motivo. Isto NÃO é conveniência: os
# arquivos guardam o cookie `t20_session` assinado, e eu já os commitei uma vez
# por rodar `playwright test --config e2e/...` de fora da pasta `e2e` (ALE-219).
/.auth/
.DS_Store
.env
.env.local
# O env de produção carrega o segredo real da mesa; o de DEV é versionado de
# propósito (valores públicos), então não entra aqui (ALE-119).
.env.production
.env.*.local
*.log

t20-book.pdf

# Runtime SQLite data + backups (host-stored, never commit)
/data/
/backups/

# Playwright MCP scratch output (page/console dumps from browser automation)
.playwright-mcp/
__pycache__/
